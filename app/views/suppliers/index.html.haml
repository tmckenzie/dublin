.scrollable_list
  %table.list
    %thead
      %tr
        %th Supplier
        %th Name
        %th
        %th
        %th

      - @suppliers.each do |supplier|
        %tr
          %td= supplier.supplier
          %td= supplier.name
          %td= link_to 'Show', supplier
          %td= link_to 'Edit', edit_supplier_path(supplier)
          %td= link_to 'Destroy', supplier, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Supplier', new_supplier_path, :class => 'blue button'
